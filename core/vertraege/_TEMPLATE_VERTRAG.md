# Übergabevertrag: <agent-a> → <agent-b>

<!-- Ein Vertrag pro Schnittstelle. Er wird VOR den beteiligten Agenten
     geschrieben und danach nur per bewusster Entscheidung geändert
     (Eintrag in docs/entscheidungen.md + beide Agenten prüfen). -->

## Wer übergibt an wen, wann
Ein Satz.

## Übergabeformat (bindend)
Exakte Struktur, die <agent-a> liefert und auf die sich <agent-b> verlässt.
Felder, Reihenfolge, Pflicht/Optional.

## Was bei fehlenden Feldern passiert
Nachfragen / Standardwert / Abbruch mit Meldung — nie stilles Raten.

## Testfälle für diese Schnittstelle
Verweis auf core/testfaelle/ketten/
